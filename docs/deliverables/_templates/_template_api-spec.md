<!--
  API仕様書テンプレート（A層 / 利用者向け）
  使い方:
    1. このファイルを 04_api-spec/<name>-api.md にコピーする
    2. 「契約・使い方・代表例」に絞る。全関数の網羅は Doxygen(C層) に委ねる
  ※ _templates/ 配下は build-docs.sh の変換対象外
-->
# API仕様書 — 〈コンポーネント名〉

| 項目 | 内容 |
|---|---|
| ドキュメント番号 | API-〈略号〉-001 |
| バージョン | 0.1 |
| ヘッダ | `#include <〈header〉.hpp>` |
| リンク | `-l〈lib〉` |
| 名前空間 | `embedded::` |

> 〈このコンポーネントが提供する価値を1〜2行で〉

---

## 1. 概要

〈クラス／関数群の役割。依存関係（例: 低レベル転送は ISpiDriver に委譲）〉

## 2. 定数 / 型

```cpp
static constexpr 〈type〉 〈NAME〉 = 〈value〉;  // 〈説明〉
```

## 3. クラス / 関数

### 3.1 〈name〉()

```cpp
[[nodiscard]] 〈ret〉 〈name〉(〈args〉) noexcept;
```

**説明**: 〈何をするか〉

| パラメータ | 型 | 説明 |
|---|---|---|
| 〈arg〉 | 〈type〉 | 〈説明〉 |

| 戻り値 | 条件 |
|---|---|
| 〈value〉 | 〈成功条件〉 |
| 〈value〉 | 〈失敗条件〉 |

## 4. 使用例

```cpp
〈最小限で動く代表例〉
```

## 5. エラーハンドリング / スレッド安全性

- 〈戻り値 / std::optional による失敗表現、例外不使用、スレッド安全性〉

## 6. バージョン履歴

| バージョン | 変更内容 |
|---|---|
| 0.1 | 初版（ドラフト） |
