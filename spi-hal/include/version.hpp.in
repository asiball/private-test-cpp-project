#pragma once

// このファイルは CMake の configure_file() が自動生成する。
// 手動で編集しないこと。編集は version.hpp.in を変更してから cmake を再実行すること。

namespace embedded {
namespace version {

// コンポーネント別バージョン（git tag から自動取得）
inline constexpr const char* SPIHAL    = "@EDS_VERSION_SPIHAL@";
inline constexpr const char* LIBSENSOR = "@EDS_VERSION_LIBSENSOR@";
inline constexpr const char* CLI       = "@EDS_VERSION_CLI@";

// ビルド時情報
inline constexpr const char* GIT_COMMIT = "@EDS_GIT_COMMIT@";
inline constexpr const char* BUILD_DATE = "@EDS_BUILD_DATE@";

} // namespace version
} // namespace embedded
